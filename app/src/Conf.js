
const Conf = {
    // automaton serilalization to localstorage will occur on change after this delay
    saveAutomatonTimeout:10000,
    //settings serilalization to localstorage will occur on change after this delay
    saveSettingsTimeout: 500,
    maxGridSide: 500,
    maxCellSize: 100,
    maxDelay: 10000,
};

export default Conf;


